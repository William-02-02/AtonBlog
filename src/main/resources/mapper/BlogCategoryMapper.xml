<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lyh.AtonBlog.dao.BlogCategoryMapper">

    <resultMap id="BaseResultMap" type="com.lyh.AtonBlog.pojo.BlogCategory">
            <id property="categoryId" column="category_id" jdbcType="INTEGER"/>
            <result property="categoryName" column="category_name" jdbcType="VARCHAR"/>
            <result property="categoryIcon" column="category_icon" jdbcType="VARCHAR"/>
            <result property="categoryRank" column="category_rank" jdbcType="INTEGER"/>
            <result property="isDeleted" column="is_deleted" jdbcType="TINYINT"/>
            <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
    </resultMap>

    <sql id="Base_Column_List">
        category_id,category_name,category_icon,
        category_rank,is_deleted,create_time
    </sql>
    
    <!--自己添加的-->
    <update id="deleteBatch">
        update tb_blog_category
        set is_deleted=1 where category_id in 
        <foreach collection="array" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </update>
    
    <select id="selectByCategoryName" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from tb_blog_category
        where category_name = #{categoryName} and is_deleted = 0
    </select>
    
    
    <select id="countCategory" parameterType="map" resultType="java.lang.Integer">
        select count(*)
        from tb_blog_category
        where is_deleted = 0
        <if test="keyword != null">
            and category_name like #{keyword} 
        </if>
    </select>
    
    <select id="findCategoryListByKeyword" parameterType="map" resultMap="BaseResultMap">
        select 
        <include refid="Base_Column_List"></include>
        from tb_blog_category
        where is_deleted = 0
        <if test="keyword != null">
            and category_name like concat('%',#{keyword},'%')
        </if>
        order by category_rank desc,create_time desc
        <if test="start != null and pageVolume != null">
            limit #{start},#{pageVolume}
        </if>
    </select>
    
    
    <!--模版生成-->
    <select id="selectByPrimaryKey"  resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List" />
        from tb_blog_category
        where  category_id = #{categoryId,jdbcType=INTEGER} 
    </select>
	<select id="selectAllByCategoryIds" parameterType="list" resultMap="BaseResultMap">
        select * from tb_blog_category
        where category_id in 
              <foreach collection="list" open="(" close=")" separator="," item="id" >
                  #{id}
              </foreach>
        and is_deleted = 0
    </select>
	
	
	<delete id="deleteByPrimaryKey" parameterType="java.lang.Long">
        update tb_blog_category
        set is_deleted = 1
        where  category_id = #{categoryId,jdbcType=INTEGER} and is_deleted=0
    </delete>
    <insert id="insert" keyColumn="category_id" keyProperty="categoryId" parameterType="com.lyh.AtonBlog.pojo.BlogCategory" useGeneratedKeys="true">
        insert into tb_blog_category
        ( category_id,category_name,category_icon
        ,category_rank,is_deleted,create_time
        )
        values (#{categoryId,jdbcType=INTEGER},#{categoryName,jdbcType=VARCHAR},#{categoryIcon,jdbcType=VARCHAR}
        ,#{categoryRank,jdbcType=INTEGER},#{isDeleted,jdbcType=TINYINT},#{createTime,jdbcType=TIMESTAMP}
        )
    </insert>
    <insert id="insertSelective" keyColumn="category_id" keyProperty="categoryId" parameterType="com.lyh.AtonBlog.pojo.BlogCategory" useGeneratedKeys="true">
        insert into tb_blog_category
        <trim prefix="(" suffix=")" suffixOverrides=",">
                <if test="categoryId != null">category_id,</if>
                <if test="categoryName != null">category_name,</if>
                <if test="categoryIcon != null">category_icon,</if>
                <if test="categoryRank != null">category_rank,</if>
                <if test="isDeleted != null">is_deleted,</if>
                <if test="createTime != null">create_time,</if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
                <if test="categoryId != null"> #{categoryId,jdbcType=INTEGER},</if>
                <if test="categoryName != null"> #{categoryName,jdbcType=VARCHAR},</if>
                <if test="categoryIcon != null"> #{categoryIcon,jdbcType=VARCHAR},</if>
                <if test="categoryRank != null"> #{categoryRank,jdbcType=INTEGER},</if>
                <if test="isDeleted != null"> #{isDeleted,jdbcType=TINYINT},</if>
                <if test="createTime != null"> #{createTime,jdbcType=TIMESTAMP},</if>
        </trim>
    </insert>
    <update id="updateByPrimaryKeySelective" parameterType="com.lyh.AtonBlog.pojo.BlogCategory">
        update tb_blog_category
        <set>
                <if test="categoryName != null">
                    category_name = #{categoryName,jdbcType=VARCHAR},
                </if>
                <if test="categoryIcon != null">
                    category_icon = #{categoryIcon,jdbcType=VARCHAR},
                </if>
                <if test="categoryRank != null">
                    category_rank = #{categoryRank,jdbcType=INTEGER},
                </if>
                <if test="isDeleted != null">
                    is_deleted = #{isDeleted,jdbcType=TINYINT},
                </if>
                <if test="createTime != null">
                    create_time = #{createTime,jdbcType=TIMESTAMP},
                </if>
        </set>
        where   category_id = #{categoryId,jdbcType=INTEGER} 
    </update>
    <update id="updateByPrimaryKey" parameterType="com.lyh.AtonBlog.pojo.BlogCategory">
        update tb_blog_category
        set 
            category_name =  #{categoryName,jdbcType=VARCHAR},
            category_icon =  #{categoryIcon,jdbcType=VARCHAR},
            category_rank =  #{categoryRank,jdbcType=INTEGER},
            is_deleted =  #{isDeleted,jdbcType=TINYINT},
            create_time =  #{createTime,jdbcType=TIMESTAMP}
        where   category_id = #{categoryId,jdbcType=INTEGER} 
    </update>
    <update id="updateRankByCategoryId" parameterType="map">
        update tb_blog_category
        set category_rank = case category_id
            <foreach collection="map.entrySet()" index="id" item="times">
                when #{id} then category_rank-#{times}
            </foreach>
            end
        where category_id = case category_id
            <foreach collection="map.entrySet()" index="id">
                when #{id} then #{id}
            </foreach>
            end
    </update>


</mapper>
